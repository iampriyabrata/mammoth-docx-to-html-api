import React from "react";

const Homepage = () => {
  return (
    <>
      <h1>Project -  # DOCX → HTML API</h1>
      <div>
        # DOCX → HTML API (Mammoth.js + Next.js) This is a simple API that
        converts `.docx` documents into **clean HTML** using
        [Mammoth.js](https://github.com/mwilliamson/mammoth.js). ## 🚀 Deploy on
        Vercel 1. Fork this repo 2. Connect it to Vercel 3. Deploy → your API
        will be live at:
      </div>
    </>
  );
};

export default Homepage;
